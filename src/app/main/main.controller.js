(function() {
  'use strict';

  angular
    .module('paringandhiInfo')
    .controller('MainController', function ($timeout, webDevTec, toastr, $rootScope) {
    var vm = this;

      $rootScope.projectName = "mainPage";

  });
})();
